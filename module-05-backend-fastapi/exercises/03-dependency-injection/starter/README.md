# Exercise 03 — starter

`models.py` and `store.py` are complete -- do not modify them. Write
`dependencies.py` and fill in the `# TODO` blocks in `main.py`, per
`INSTRUCTIONS.md`.

```bash
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
uvicorn main:app --reload
```
