# Exercise 01 — starter

Set up your own venv here first (per `lessons/00-setup.md`):

```bash
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
```

Then create `main.py` in this same folder per `INSTRUCTIONS.md`, and run it with:

```bash
uvicorn main:app --reload
```
