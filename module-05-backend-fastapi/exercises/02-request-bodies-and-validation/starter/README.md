# Exercise 02 — starter

Set up your venv, install `requirements.txt`, then build `main.py` per `INSTRUCTIONS.md`.
Fill in `VALIDATION_NOTES.md` (create it in this folder) with what you actually observe.
