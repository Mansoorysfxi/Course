# Exercise 04 — starter

`models.py` and `store.py` are complete -- do not modify them. Fill in every
`# TODO` in `main.py` per `INSTRUCTIONS.md`.

```bash
python -m venv .venv
source .venv/Scripts/activate
pip install -r requirements.txt
uvicorn main:app --reload
```
