// TODO (Exercise 04, Step 4): the route table
// See lessons/08-react-router.md.
//
// `<BrowserRouter>` is already set up in main.tsx, wrapping <App />, so
// you don't need it here -- App.tsx only needs `<Routes>` and `<Route>`.
//
// Build three routes:
//   - "/" (index) -> <CatalogPage />
//   - "/items/:id" -> <ItemDetailPage />
//   - "*" (catch-all) -> <NotFoundPage />
//
// import { Routes, Route } from "react-router";
// import { CatalogPage } from "./pages/CatalogPage";
// import { ItemDetailPage } from "./pages/ItemDetailPage";
// import { NotFoundPage } from "./pages/NotFoundPage";

function App() {
  return <p className="muted">TODO: build the route table (see the comments above).</p>;
}

export default App;
