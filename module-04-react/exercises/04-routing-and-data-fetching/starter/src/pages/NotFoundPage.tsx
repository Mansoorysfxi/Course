// TODO (Exercise 04, Step 3): NotFoundPage
// See lessons/08-react-router.md's catch-all route section.
//
// A small page shown for any URL that doesn't match a real route.
// Render a heading (e.g. "404 -- Page Not Found") and a
// <Link to="/">Back to catalog</Link>.
export function NotFoundPage() {
  return <p className="muted">TODO: build NotFoundPage (see the comments above).</p>;
}
