// TODO (Exercise 01, Step 1): PriorityBadge
//
// See lessons/01-why-react-components-props-and-jsx.md for everything
// this file needs.
//
// Build a component named `PriorityBadge` that:
//   1. Accepts exactly one prop, `priority`, typed as
//      "low" | "medium" | "high" (a union of string literals -- you saw
//      this exact pattern for TypeScript unions in Module 03, Lesson 09,
//      and it reappears constantly in React prop types).
//   2. Renders a <span> with className="badge" (that CSS class already
//      exists in src/index.css) containing the priority text.
//
// Reminder of the shape every component in this exercise follows (from
// the lesson): a function, capitalized, that takes ONE argument (an
// object of props, destructured), and returns JSX.
//
// export function PriorityBadge({ priority }: SomePropsType) {
//   return <span className="badge">{priority}</span>;
// }
//
// Delete everything below this line and write the real thing.

export function PriorityBadge() {
  return null;
}
