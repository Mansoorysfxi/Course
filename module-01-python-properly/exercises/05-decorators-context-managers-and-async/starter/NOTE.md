No pre-written skeleton for this exercise — build `toolkit.py` entirely
from scratch, per `INSTRUCTIONS.md`. This folder exists only to keep the
module layout consistent. Create your file as
`starter/toolkit.py` once you start.
