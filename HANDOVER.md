# Handover — Course Generation Status

**Read this file first if you are an AI session picking up this repo's
generation work.** It tells you exactly what's done, what's left, the
process that's been working, and the specific gotchas already hit so you
don't repeat them. This file is about *generating the course*, not about
the learner's progress — that's [PROGRESS.md](PROGRESS.md), a separate,
still-empty file the learner's actual study sessions will fill in later.

Last updated: after Module 08 completed (this handover written immediately
after, per explicit user instruction to generate Module 08 only, then
update this file and stop).

## Status at a glance

| Module | Status |
|---|---|
| Root files (README, PROGRESS, GRADING_PROTOCOL, GLOSSARY, MASTER_LEARNING_PLAN, RUNNING_PROJECT) | ✅ Done |
| Folder skeleton for all 16 modules (00–15) | ✅ Done |
| 00 — Developer Environment & Tooling | ✅ Fully generated |
| 01 — Python, Properly | ✅ Fully generated |
| 02 — Internet & Web Fundamentals | ✅ Fully generated |
| 03 — HTML, CSS, JavaScript | ✅ Fully generated |
| 04 — React (QuestLog begins) | ✅ Fully generated |
| 05 — Backend with FastAPI | ✅ Fully generated |
| 06 — Databases | ✅ Fully generated (see "Known issues already hit" — this one needed manual repair) |
| 07 — Auth, Security & API Best Practices | ✅ Fully generated and verified on disk |
| 08 — Testing & Software Quality (Review Project) | ✅ Fully generated and verified on disk — test suites actually run and pass, see below |
| 09 — Linux, Networking & Servers | ❌ Not started — **next up** |
| 10 — Docker & Containers | ❌ Not started |
| 11 — CI/CD, Cloud & Production Operations | ❌ Not started |
| 12 — AI/ML Foundations | ❌ Not started |
| 13 — Building with LLM APIs | ❌ Not started |
| 14 — RAG | ❌ Not started |
| 15 — Agents & Modern AI Workflows (Final Capstone) | ❌ Not started |

The root `README.md`'s checkbox progress table and `PROGRESS.md` are the
**learner's** trackers — they intentionally still show everything
unchecked/not-started, because no learner has actually studied any module
yet. Do not "helpfully" check boxes there to reflect generation progress —
that would misrepresent the learner's real status. This handover file is
the correct place for generation status.

## The process that's been working

Each module 01–08 was generated by spawning a single `general-purpose`
Agent per module (via the `Agent` tool) with a long, detailed prompt that:

1. Points it at `MASTER_LEARNING_PLAN.md` (the rules) and `RUNNING_PROJECT.md` (the QuestLog spec).
2. Points it at the *previous* module's finished `project/questlog/` as the exact starting codebase to copy forward and extend (never regenerate from scratch).
3. Points it at 1–2 prior modules' `lessons/00-setup.md` files as a tone/depth calibration example.
4. Spells out that specific module's curriculum from the master plan, with a suggested lesson-file breakdown.
5. Explicitly requires real web research (Rule 7) for anything version/library-specific, and names the specific things to verify.
6. Requires a self-audit against Rules 1/5/6/7/8/9 before reporting done.
7. Requires appending new terms to `GLOSSARY.md` **in correct alphabetical position** (the file is large — ~1700 lines — so this means finding the right spot via Grep/Read, not just appending at the end).
8. Explicitly forbids touching any other module's folder or any root file besides `GLOSSARY.md`.
9. Asks for a final report under ~450 words: files created, versions verified with dates, what was live-tested vs. hand-verified, and the self-audit result.

**After every agent call finishes, verify on disk before trusting the
report** — this repo has already had one case (Module 06) where the
agent's self-report and the actual on-disk state disagreed after a
connection failure. See below. A cheap verification pattern:
```bash
find module-XX-name -type f | sort          # does the file list look complete/sane?
find module-XX-name -type d -name node_modules -o -type d -name __pycache__ -o -type d -name ".venv*"   # any stray build artifacts to delete?
grep -c "Module XX" GLOSSARY.md              # did glossary entries actually land?
```

## Known issues already hit (so you don't repeat them)

1. **Long agent calls can get cut off mid-response** ("Connection closed
   mid-response" / API errors). When this happens, **do not restart from
   scratch** — use `SendMessage` to the same `agentId` (given in the failed
   call's result) telling it to re-check the current on-disk state of its
   module folder and continue from wherever it actually left off, then
   re-run its own self-audit and give a fresh final report. This worked
   cleanly for both Module 05 and Module 07's interruptions.
2. **A session-wide usage limit can be hit**, which fails *every* agent
   call (not just one) until it resets — the error explicitly states a
   reset time. If you hit this, there's no point retrying immediately;
   either wait, or fall back to writing lesson/exercise files directly
   yourself with `Write`/`Edit` (slower, but works — this is exactly how
   Module 06 got finished after its second connection failure coincided
   with a session limit).
3. **Module 06 specifically needed manual repair** after its interrupted
   agent run: it had left a stray `.venv-test/` folder (a full installed
   virtualenv, deleted), a `requirements.txt` missing `sqlalchemy`/
   `alembic`/`asyncpg` despite the code importing them, and two `README.md`
   files still describing themselves as "Module 05." All of Module 06's
   `lessons/`, `exercises/`, `CHECKLIST.md`, `README.md`, and
   `project/BRIEF.md` were then written by hand (not via agent) to finish
   it. **Lesson: always verify a module's file list and skim its READMEs
   for stale "Module NN" references after any interrupted run, before
   trusting it's done.**
4. **Stray build artifacts.** Agents that actually run/test code (installing
   a venv, running `npm install`, etc.) sometimes forget to delete the
   result. Always grep for `node_modules`, `__pycache__`, `.venv*` inside a
   newly generated module and delete any that snuck in — they don't belong
   in the course repo.
5. **`Read`/`Bash` on files matching certain patterns (e.g. `.env*`) can be
   permission-denied** by the harness even for legitimate inspection — this
   is expected sandboxing, not a bug; work around it by reading adjacent
   non-sensitive files (`.gitignore`, `README.md`) instead.
6. **The session-usage limit (issue 2) was hit a second time, mid-Module
   08**, cutting the agent off right after it had copied Module 07's
   `project/questlog/` forward but before it had written any lessons,
   exercises, or the test suite itself. The same recovery worked again:
   `SendMessage` to the same `agentId`, telling it exactly what was
   verified as already on disk (so it wouldn't redo or second-guess the
   copy step) and exactly what was still missing, plus a reminder to clean
   up a stray `.venv-check/` (114MB) it had left from verification work.
   It resumed cleanly, cleaned up the stray folder itself, and finished
   the entire rest of the module. **Lesson confirmed twice now: resuming
   with a precise "here's what's actually on disk, here's what's missing"
   message is reliable — don't restart a module from scratch after a
   session-limit or connection failure.**

## The running project: QuestLog

Full spec in [RUNNING_PROJECT.md](RUNNING_PROJECT.md) — read it before
generating any further module. Quick recap of where the codebase actually
stands after Module 08:

- **Frontend:** React 19 + TypeScript 7 + Vite 8 + Tailwind CSS 4 + React
  Router 8. Real login/signup flow, JWT stored and sent as a Bearer token,
  unauthenticated users redirected away from protected pages. **New in
  Module 08:** a real Vitest + React Testing Library suite (4 test files,
  17 tests, all passing) covering `QuestForm`, `QuestCard`,
  `ProtectedRoute`, and `QuestListPage`'s loading/error/success states,
  plus Prettier config.
- **Backend:** FastAPI 0.141.x + Pydantic 2.13.x + Uvicorn, PostgreSQL 18
  via SQLAlchemy 2.0.51 (async) + Alembic 1.19.0 + asyncpg. Real signup/
  login endpoints, bcrypt password hashing, PyJWT-based JWTs, every quest
  route requires auth and is scoped to `owner_id` (the `User` table and
  `owner_id` foreign key were deliberately added back in Module 06, one
  module early — see `module-06-databases/lessons/10-designing-questlogs-schema.md`).
  **New in Module 08:** a real pytest suite (`tests/conftest.py`,
  `test_security.py`, `test_auth.py`, `test_quests.py` — 31 tests, all
  passing) plus `ruff` (linting/formatting), and a repo-root
  `.pre-commit-config.yaml` wired to both `ruff` and `prettier` (verified
  by actually running `pre-commit run --all-files` — this caught and fixed
  several genuine style issues in Module 07's original code, now corrected).
- **Test-database strategy (a real decision future modules should know
  about):** Module 08 chose **in-memory SQLite** for the backend test
  suite, not a dedicated Postgres test database — justified in
  `module-08-testing-and-quality/lessons/06-testing-with-a-database.md`
  because QuestLog's schema uses no Postgres-specific types. If a future
  module (e.g. 09/10/11 touching deployment/CI) needs the test suite to
  run in a pipeline, this is why no second Postgres instance is required
  for tests specifically — only for the app itself.
- **Latest finished reference codebase:**
  `module-08-testing-and-quality/project/questlog/` (`backend/` + `frontend/`,
  both with real, passing test suites). **Module 09 must copy this exact
  folder forward** into `module-09-linux-networking-servers/project/questlog/`
  — do not regenerate the app from scratch.

## Next up: Module 09 — Linux, Networking & Servers

Curriculum per the master plan: Linux deeper (processes, permissions,
systemd, SSH/key-based auth, package managers); networking for developers
(ports, localhost, private vs public IPs, firewalls, reverse proxies —
what Nginx does and why, load balancers conceptually); then **deploy the
Module 08 QuestLog app manually to a cheap VPS, on purpose, the painful
way**, so Module 10 (Docker) and Module 11 (CI/CD) later feel like relief
rather than magic. This module does not add QuestLog *features* — it
changes *where and how it runs*.

**Before generating it:**
1. Skim `module-08-testing-and-quality/project/questlog/` (just enough to
   know what needs to run on a server: Postgres, the FastAPI backend via
   Uvicorn, and how the frontend gets served/built) — no code changes are
   expected to QuestLog itself in this module, only deployment steps.
2. Web-research (Rule 7): a concrete, currently cheap VPS provider to name
   as the example (the master plan says "a cheap VPS" without naming one —
   pick one with current, verifiable pricing and say so), current Ubuntu
   LTS version to install, current `systemd` service-file conventions,
   current Nginx stable version and reverse-proxy config syntax, current
   SSH key-generation guidance (`ssh-keygen` algorithm recommendations
   have shifted over time — verify, don't assume `rsa` is still the
   default suggestion).
3. This module's "exercises" and "capstone" are unusually hands-on-a-real-
   server rather than hands-on-more-code — the master plan's own framing
   ("on purpose, the painful way") suggests the lessons should be honest
   about that friction rather than smoothing it over, since Module 10
   explicitly exists to fix the pain this module intentionally creates.
4. Use the same Agent-delegation process described above ("The process that's been working").

## After Module 09

Continue the same pattern through Modules 10–15, always copying the
previous module's `project/questlog/` forward per `RUNNING_PROJECT.md`'s
table. Module 10 (Docker) containerizes what Module 09 just did by hand.
Modules 12 and 15 are two points worth flagging in advance:
- **Module 12 (AI/ML Foundations)** is explicitly concept-only per the
  master plan and `RUNNING_PROJECT.md` — no QuestLog code changes, standalone
  exercises (tokenization, embeddings, prompting). Don't force QuestLog
  integration where the plan deliberately doesn't want it yet.
- **Module 15** is the course's **final capstone** — budget real care for
  it; it's the portfolio piece the entire course has been building toward.

## Root-file maintenance as modules are added

Each new module's agent is told to update only `GLOSSARY.md`. After
Module 15 is eventually done, someone (agent or you) still needs to do a
final pass — listed already in the standing todo list at the time of this
handover — to update the root `README.md`'s checkbox table's *structure*
if needed (it already lists all 16 modules correctly, so likely no change
needed there) and to spot-check cross-module links/consistency across the
whole repo. That final pass has not happened yet.
